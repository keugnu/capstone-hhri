Node:	hug (Speech response to hugging force)
Input:
	uint16 pressure // need to make sure >=2 force sensors triggered, on opposite sides of torso.
Output:
	String hug_response: "I love you"
Steps:
	Initialization: begin subscribing to tpc_force (10Hz)
	1. if more than one force input registered then:
	2. if input from force sensors is greater than certain amount then:
		(a) Play hug_response audio
