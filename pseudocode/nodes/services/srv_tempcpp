Node:   temp
Input:
    None
Output:
    int16 temp (C)
Steps:
    Initialization: Wait for requests
    1.  if request is received, then
        (a) send request to I2C Bus Manager (R,addr)
        (b) Convert uint16 from sensor to degrees C
        (b) return temp
