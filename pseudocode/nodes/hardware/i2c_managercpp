Node:	i2c_manager
Input:
	char type ['R','W']
	int16 address
	... (variadic)
Config:
	int8 address
Output:
	bool success
Steps:
	1. 	Initialization: Check if bus is able to be opened else exit(1)
	2.	Wait for request
	3. 	if request exists, then
			(a) place on request queue
	4.	if queue is not empty
			(a) serve top request
				(i) 	Determine type and use address to find required args
				(ii)	Perform request
	5.	if request failed, then
			(a) return true
		else
			(a) Place request back onto queue
			(b) return false